import SwiftUI

/// Placeholder until Stage 28 lands this section.
struct VocabularySection: View {
    let model: SettingsModel

    var body: some View {
        SettingsPane(title: "Vocabulary") {
            SettingsFootnote("This section arrives in Stage 28.")
        }
    }
}
